<EditForm Model="SupplierModel">
    <DataAnnotationsValidator />
        <div class="border rounded-lg p-4 shadow-sm bg-white financial__information">
            <div class="d-flex justify-content-between ">
                <h3 class="header-text">Financial Information</h3>
            </div>

            <div class="row ">
                <div class="col-lg-12 col-md-12 col-sm-12 mt-3 ">
                    <div class="border rounded-sm  text-dark pl-3 pr-3 ">
                        <h5 class="mt-2 text-muted input-text">BANK NAME</h5>
                        @if (ModuleType == "Add" || ModuleType == "Edit")
                        {
                        <InputText @bind-Value="SupplierModel.BankName" class="form-control mb-2 form-control-sm"
                            id="bank_name" placeholder="Enter Bank Name" name="bank_name"></InputText>
                        <ValidationMessage For="@(() => SupplierModel.BankName)" />
                        }
                        else    
                        {
                            <p class="tex   t-muted input-field-text" contentEditable="true" placeholder="">@SupplierModel.BankName</p> 
                        }
                    </div>
                </div>

                <div class="col-lg-6 col-md-12 col-sm-12 mt-3 ">
                    <div class="border rounded-sm  text-dark pl-3 pr-3 ">
                        <h5 class="mt-2 text-muted input-text">ACCOUNT NUMBER</h5>
                        @if (ModuleType == "Add" || ModuleType == "Edit")
                        {
                        <InputText @bind-Value="SupplierModel.AccountNumber" class="form-control mb-2 form-control-sm"
                            id="account_number" placeholder="Enter Account Number" name="account_number"></InputText>
                        <ValidationMessage For="@(() => SupplierModel.AccountNumber)" />
                        }
                        else
                        {
                            <p class="text-muted input-field-text" contentEditable="true" placeholder="">@SupplierModel.AccountNumber</p> 
                        }
                    </div>
                </div>

                <div class="col-lg-6 col-md-12 col-sm-12 mt-3 ">
                    <div class="border rounded-sm  text-dark pl-3 pr-3 ">
                        <h5 class="mt-2 text-muted input-text">ACCOUNT NAME</h5>
                        @if (ModuleType == "Add" || ModuleType == "Edit")
                        {
                        <InputText @bind-Value="SupplierModel.AccountName" class="form-control mb-2 form-control-sm"
                            id="account_name" placeholder="Enter Account Name" name="account_name"></InputText>
                        <ValidationMessage For="@(() => SupplierModel.AccountName)" />
                        }
                        else
                        {
                        <p class="text-muted input-field-text" contentEditable="true" placeholder="">@SupplierModel.AccountName</p> 
                        }
                    </div>
                </div>

                <div class="col-lg-6 col-md-12 col-sm-12 mt-3 ">
                    <div class="border rounded-sm  text-dark pl-3 pr-3 ">
                        <h5 class="mt-2 text-muted input-text">SWIFT</h5>
                        @if (ModuleType == "Add" || ModuleType == "Edit")
                        {
                        <InputText @bind-Value="SupplierModel.Swift" class="form-control mb-2 form-control-sm" id="swift"
                            placeholder="Enter Swift" name="swift"></InputText>
                        <ValidationMessage For="@(() => SupplierModel.Swift)" />
                        }
                        else
                        {
                            <p class="text-muted input-field-text" contentEditable="true" placeholder="">@SupplierModel.Swift</p>
                        }
                    </div>
                </div>

                <div class="col-lg-6 col-md-12 col-sm-12 mt-3 ">
                    <div class="border rounded-sm  text-dark pl-3 pr-3 ">
                        <h5 class="mt-2 text-muted input-text">ADDRESS</h5>
                        @if (ModuleType == "Add" || ModuleType == "Edit")
                        {
                        <InputText @bind-Value="SupplierModel.Address" class="form-control mb-2 form-control-sm" id="address"
                            placeholder="Enter Address" name="address"></InputText>
                        <ValidationMessage For="@(() => SupplierModel.Address)" />
                        }
                        else
                        {
                            <p class="text-muted input-field-text" contentEditable="true" placeholder="">@SupplierModel.Address</p>
                        }
                    </div>
                </div>
            </div>           
        </div>
</EditForm>